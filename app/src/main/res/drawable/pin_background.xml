<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Degradado de arriba hacia abajo: morado de marca -> fondo oscuro de la app -->
    <gradient
        android:type="linear"
        android:angle="270"
        android:startColor="@color/brand_purple"
        android:centerColor="@color/brand_purple_soft"
        android:endColor="@color/bg_deep" />

</shape>
